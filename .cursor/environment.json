{
  "name": "MQIS",
  "install": "python3 -m venv .venv && . .venv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt",
  "terminals": [
    {
      "name": "streamlit",
      "command": ". .venv/bin/activate && streamlit run app.py --server.port 8501 --server.address 0.0.0.0 --server.headless true"
    }
  ],
  "ports": [
    {
      "name": "Streamlit dashboard",
      "port": 8501
    }
  ]
}
