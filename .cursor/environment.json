{
  "name": "MQIS",
  "install": "sudo apt-get update && sudo apt-get install -y python3-venv && python3 -m venv .venv && . .venv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt",
  "terminals": [
    {
      "name": "streamlit",
      "command": ". .venv/bin/activate && streamlit run app.py --server.port 8501 --server.address 0.0.0.0 --server.headless true",
      "description": "MQIS Streamlit dashboard on http://localhost:8501 (Market/Index + Sector Quant pages)."
    }
  ],
  "ports": [
    {
      "name": "Streamlit dashboard",
      "port": 8501
    }
  ]
}
